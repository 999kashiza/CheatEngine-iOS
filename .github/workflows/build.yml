name: Build iOS Tweak

on:
  workflow_dispatch:  # Déclenchement manuel
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v4  # ← Mis à jour v4
    
    - name: Setup Theos
      run: |
        bash -c "$(curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos)"
        echo "THEOS=$HOME/theos" >> $GITHUB_ENV
    
    - name: Build Package
      run: |
        export THEOS=$HOME/theos
        make package FINALPACKAGE=1
    
    - name: Extract dylib
      run: |
        mkdir -p output
        # Extraire le dylib du deb généré
        dpkg-deb -x packages/*.deb extracted/
        cp extracted/Library/MobileSubstrate/DynamicLibraries/*.dylib output/
        cp extracted/Library/MobileSubstrate/DynamicLibraries/*.plist output/
        ls -la output/
    
    - name: Upload Artifact
      uses: actions/upload-artifact@v4  # ← CORRIGÉ : v4 au lieu de v3
      with:
        name: cemobile-dylib
        path: output/
        retention-days: 30
